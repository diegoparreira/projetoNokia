import React from 'react';
import { Screen } from './styles';

const handleChange = () => {
    
}

const Screen = (text) => {
    return (
        <div>
            {text}
        </div>
    )
}

export default Screen;